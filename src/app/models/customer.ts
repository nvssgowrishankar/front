export interface Customer {
    customerId?: number; // Optional if not required for creation
    username: string;
    password: string;
    email: string;
    address: string;
    mobileNumber: string;
  }
  